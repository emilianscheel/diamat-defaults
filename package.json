{
  "name": "diamat-defaults",
  "version": "1.0.0",
  "description": "Dieses Script generiert Beispieldaten für den Dia-Mat.",
  "main": "src/index.js",
  "scripts": {
    "run": "node ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emilian Scheel",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1"
  }
}
